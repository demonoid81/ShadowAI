apiVersion: v1
kind: Service
metadata:
  name: {{ include "shadowai.fullname" . }}
  labels:
    {{- include "shadowai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "shadowai.selectorLabels" . | nindent 4 }}
