# This template is intentionally empty.
# Create the secret out-of-band before installing the chart:
#
#   kubectl create secret generic shadowai-secrets \
#     --from-literal=databaseUrl='postgres://user:pass@host:5432/db?sslmode=require' \
#     --from-literal=jwtSecret='<strong-random-secret-32+chars>' \
#     --from-literal=redisUrl='redis://:password@redis:6379/0'
#
# For production, use external-secrets-operator or Vault to inject secrets
# from your secrets manager. Never commit plaintext secrets to this file.
#
# The deployment.yaml references {{ .Values.existingSecret }} for all
# sensitive values.
